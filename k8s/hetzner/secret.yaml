apiVersion: v1
kind: Secret
metadata:
  name: hetzner-s3-credentials
type: Opaque
data:
  # Base64 encode your Hetzner Object Storage credentials
  # echo -n "your-access-key" | base64
  # echo -n "your-secret-key" | base64
  access-key: "CHANGE_ME_BASE64_ENCODED"
  secret-key: "CHANGE_ME_BASE64_ENCODED"