# NOTE: Replace this manual secret with ExternalSecret for production
# Example ExternalSecret configuration:
#
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: hetzner-s3-credentials-external-secret
# spec:
#   secretStoreRef:
#     name: your-secret-store
#     kind: SecretStore
#   target:
#     name: hetzner-s3-credentials
#     creationPolicy: Owner
#   data:
#   - secretKey: access-key
#     remoteRef:
#       key: "HETZNER_S3_ACCESS_KEY_UUID"
#   - secretKey: secret-key
#     remoteRef:
#       key: "HETZNER_S3_SECRET_KEY_UUID"

# Temporary manual secret (replace with ExternalSecret above)
apiVersion: v1
kind: Secret
metadata:
  name: hetzner-s3-credentials
type: Opaque
stringData:
  # Add your actual Hetzner Object Storage credentials here
  # No base64 encoding needed with stringData
  access-key: "YOUR_ACCESS_KEY_HERE"
  secret-key: "YOUR_SECRET_KEY_HERE"